<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".DigSigApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigSig">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DigSig">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".KeyFactoryActivity"
            android:exported="false"
            android:label="@string/key_pair_generator"
            android:theme="@style/Theme.DigSig" />
        <activity
            android:name=".SignDocumentActivity"
            android:exported="false"
            android:label="@string/sign_document"
            android:theme="@style/Theme.DigSig" />
        <activity
            android:name=".VerifySignatureActivity"
            android:exported="false"
            android:label="@string/verify_signature"
            android:theme="@style/Theme.DigSig" />
    </application>

</manifest>